export default class User {
    constructor(name, role, videoStream, voiceStream) {
      this.name = name;
      this.role = role;
      this.videoStream = videoStream;
      this.voiceStream = voiceStream;
    }
  }